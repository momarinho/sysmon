name: sysmon_dashboard
description: System Monitor Dashboard - Flutter Desktop

publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  # Charts
  fl_chart: ^0.65.0

  # Icons
  material_design_icons_flutter: ^7.0.0

  # Utilities
  intl: ^0.19.0
  web_socket_channel: ^3.0.0
  google_fonts: ^6.1.0
  riverpod: ^2.4.0
  flutter_riverpod: ^2.4.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  test: ^1.24.0

flutter:
  uses-material-design: true
